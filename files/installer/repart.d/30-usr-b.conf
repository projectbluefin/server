# USR-B — the A/B /usr rollback slot.
# Type GUID: Flatcar's usr partition, 5dfbf5f4-2848-4bac-aa5e-0d9a20b745a6,
# adopted verbatim by projectbluefin/server#134. Same GUID as USR-A so the
# kernel's usr= slot logic recognises the pair; sized to match USR-A.
# Empty and unformatted in this ticket: wiring sysupdate to stage updates
# into USR-B is the sysupdate follow-on.
[Partition]
Type=5dfbf5f4-2848-4bac-aa5e-0d9a20b745a6
Label=USR-B
SizeMinBytes=4G
