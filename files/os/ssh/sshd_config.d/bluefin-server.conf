# SSH configuration for Bluefin Server.
# Hardened defaults: no direct root password login, no password auth at all.
# Bring-up diagnostics must use key-based auth (inject an authorized_keys
# entry via systemd credentials or the installer).

PermitRootLogin prohibit-password
PubkeyAuthentication yes
PasswordAuthentication no
KbdInteractiveAuthentication no
